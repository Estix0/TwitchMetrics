version: '3.8'

services:
  tracker_channel1:
    build: .
    container_name: tracker_channel1
    restart: unless-stopped
    env_file: 
      - .env.channel1
    ports:
      - "8501:8501"
    volumes:
      - ./data/channel1:/data
